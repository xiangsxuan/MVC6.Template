@using Links = MvcTemplate.Resources.Views.AccountView.Texts
@using MvcTemplate.Resources.Views.AccountRegisterView

@model AccountRegisterView

<div class="title text">@ContentTitles.AuthRegister</div>
<div class="separator"></div>

<form asp-antiforgery="true">
    <input type="password" style="display: none" /> @* Forcing browser to not remember following form inputs *@
    @Html.Partial("_AuthAlerts")

    <div class="auth-content">
        <div class="input-group">
            <span class="fa fa-user"></span>
            <input asp-for="Username" placeholder="@Titles.Username" autocomplete="off" />
        </div>
        <div class="input-group">
            <span class="fa fa-lock"></span>
            <input asp-for="Username" type="password" placeholder="@Titles.Username" autocomplete="off" />
        </div>
        <div class="input-group">
            <span class="fa fa-envelope"></span>
            <input asp-for="Email" placeholder="@Titles.Email" autocomplete="off" />
        </div>
    </div>
    <input class="btn btn-primary" type="submit" value="@Actions.Register" />
</form>

<div class="separator"></div>
<div class="auth-actions">
    <a asp-action="Login" class="text fa fa-sign-in" data-toggle="tooltip" data-original-title="@Links.Login" data-placement="bottom"></a>
    <a asp-action="Recover" class="text fa fa-unlock-alt" data-toggle="tooltip" data-original-title="@Links.LostPassword" data-placement="bottom"></a>
</div>